"use client";
import {useState} from "react";import {searchMemories} from "../../lib/api";
export default function MemoryPage(){const [query,setQuery]=useState("");const [results,setResults]=useState<any[]>([]);const search=async()=>{if(query.trim())setResults((await searchMemories(query)).results)};return <div className="stack"><div className="row"><input className="input" value={query} onChange={e=>setQuery(e.target.value)} onKeyDown={e=>e.key==="Enter"&&search()} placeholder="Search encrypted memories…"/><button className="button" onClick={search}>Search</button></div>{results.map((x,i)=><article className="card" key={x.id||i}><span className="tag">{x.category||"memory"}</span><p>{x.preview||x.content}</p><small className="muted">{x.created_at||""}</small></article>)}{!results.length&&<div className="card muted">Search your memories by meaning or keyword.</div>}</div>}