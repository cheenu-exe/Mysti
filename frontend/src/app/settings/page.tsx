"use client";
import {useState} from "react";
export default function SettingsPage(){const [level,setLevel]=useState("T0");return <div className="stack"><section className="card stack"><div><div className="eyebrow">Security</div><h2>Trust level</h2></div><select className="select" value={level} onChange={e=>setLevel(e.target.value)}>{["T0","T1","T2","T3","T4","T5"].map(x=><option key={x}>{x}</option>)}</select><p className="muted">Higher trust levels enable additional network and tool capabilities.</p><button className="button" style={{background:"#ff5570"}}>Emergency revoke all</button></section><section className="card stack"><div className="eyebrow">Model configuration</div><input className="input" placeholder="Provider"/><input className="input" type="password" placeholder="API key (masked)"/><button className="button">Save settings</button></section></div>}