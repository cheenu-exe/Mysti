/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  output: 'export',
  basePath: '/Mysti',
  assetPrefix: '/Mysti/',
  images: { unoptimized: true },
};

export default nextConfig;